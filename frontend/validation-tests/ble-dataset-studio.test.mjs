import test from 'node:test';import assert from 'node:assert/strict';
import {summarizeExamples,splitHasLeakage,splitScientificStatus,summarizeProtocolProgress} from '../.ble-validation/datasetStudioModel.js';
test('dataset summary keeps included and quarantined examples separate',()=>{const result=summarizeExamples([{session_id:'s1',crc_valid:true,inclusion_state:'INCLUDED_STRONG'},{session_id:'s1',crc_valid:false,inclusion_state:'QUARANTINED_SESSION_LOSS'}]);assert.deepEqual(result,{total:2,crcValid:1,included:1,excluded:0,quarantined:1,sessions:1})});
test('group split detects session leakage',()=>{assert.equal(splitHasLeakage({train:{s1:['a']},validation:{s2:['b']},test:{s1:['c']}}),true);assert.equal(splitHasLeakage({train:{s1:['a']},validation:{s2:['b']},test:{s3:['c']}}),false)});
test('historical 3 second sessions do not satisfy a frozen 30 second protocol',()=>{const sessions=Array.from({length:3},()=>({executedDurationSeconds:3,requiredDurationSeconds:30,metadataComplete:false,qualityClean:false,acceptedExamples:0,historicalImport:true}));assert.deepEqual(summarizeProtocolProgress(sessions,12),{planned:12,executed:3,conformant:0,historicalNonconformant:3,accepted:0})});
test('split integrity does not imply training readiness',()=>{assert.deepEqual(splitScientificStatus(0,'PASSED'),{splitIntegrityCheck:'PASSED',splitStatus:'NOT_READY',trainingUsable:false,reason:'no_accepted_examples'})});
